[project]
name = "py-wal"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "msgpack>=1.1.1",
]

[dependency-groups]
dev = [
    "black>=25.1.0",
]
